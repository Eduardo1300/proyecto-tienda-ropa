{
  "buildCommand": "cd tienda-vue && npm install && npm run build && mv dist ../dist",
  "devCommand": "cd tienda-vue && npm run dev",
  "installCommand": "npm ci",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
